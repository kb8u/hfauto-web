{
  # the IP address and UDP port number that the W1TR HFauto application
  # is exporting to.  Defaults to a non-localhost IP and port 15080 if these
  # options are missing or commented out
  w1tr_ip => '127.0.0.1',
  w1tr_port => 15080,
}
